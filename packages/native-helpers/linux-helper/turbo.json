{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["src/**", "resources/**", "Cargo.toml", "Cargo.lock"],
      "outputs": ["target/release/linux-helper", "bin/linux-helper"],
      "cache": true
    }
  }
}
